import styles from '../styles/footer.module.css';

const Header = () => {
    return (
        <footer className={styles.footer}>
                <span>
                    Direitos reservados &copy; 2024
                </span>
        </footer>
    );
};

export default Header;